cars = ['bmw', 'audi', 'toyota', 'subaru']
#The sort() method, shown, changes the order of the list perma- nently. The cars are now in alphabetical order, and we can never revert to the original order:
cars.sort(reverse=True)
print(cars)
